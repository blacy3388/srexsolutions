const menuButton=document.querySelector(".menu-toggle");const nav=document.querySelector(".main-nav");const dropdown=document.querySelector(".nav-dropdown");const dropdownButton=document.querySelector(".dropdown-toggle");if(menuButton){menuButton.addEventListener("click",()=>{const open=nav.classList.toggle("open");menuButton.setAttribute("aria-expanded",String(open))})}if(dropdownButton){dropdownButton.addEventListener("click",e=>{if(window.innerWidth<=860){e.preventDefault();dropdown.classList.toggle("open")}})}document.querySelectorAll(".main-nav a").forEach(link=>link.addEventListener("click",()=>{nav.classList.remove("open");dropdown.classList.remove("open")}));const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}})},{threshold:.1});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));document.querySelectorAll("#year").forEach(el=>el.textContent=new Date().getFullYear());const requestedPackage=new URLSearchParams(window.location.search).get("package");if(requestedPackage){const serviceSelect=document.querySelector('select[name="service"]');if(serviceSelect){const normalised=requestedPackage.toLowerCase()==="consultation"?"Package consultation":requestedPackage;const matchingOption=Array.from(serviceSelect.options).find(option=>option.text.toLowerCase()===normalised.toLowerCase());if(matchingOption)serviceSelect.value=matchingOption.value}const message=document.querySelector('textarea[name="message"]');if(message&&!message.value)message.placeholder=`Tell us about your property and requirements for ${requestedPackage}.`}
